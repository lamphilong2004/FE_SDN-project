<nav class="navbar navbar-expand-lg navbar-dark bg-primary shadow-sm">
    <div class="container">
        <a class="navbar-brand fw-bold" href="/">QuestionBank</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav ms-auto">
                <li class="nav-item">
                    <a class="nav-link" href="/quizzes">Quizzes</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/questions">Questions</a>
                </li>
                <% if (isAuthenticated) { %>
                    <li class="nav-item">
                        <a class="nav-link" href="/logout">Logout</a>
                    </li>
                <% } else { %>
                    <li class="nav-item">
                        <a class="nav-link" href="/login?returnTo=<%= encodeURIComponent(typeof originalUrl === 'string' ? originalUrl : '/') %>">Login</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/register?returnTo=<%= encodeURIComponent(typeof originalUrl === 'string' ? originalUrl : '/') %>">Register</a>
                    </li>
                <% } %>
            </ul>
        </div>
    </div>
</nav>